-- 0018 スペース別の通知メール宛先（#72）
-- 提携店など、スペースごとに管理者通知を届けたい先を1つ設定できる。
-- 複数人への配信はメールサーバー側の転送（エイリアス）で行う想定のため、システムは1アドレスのみ保持。
-- 空欄なら従来どおり本部（MAIL_ADMIN）のみに通知する。
ALTER TABLE spaces ADD COLUMN notify_email TEXT;
