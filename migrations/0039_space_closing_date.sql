-- 0039: スペース別「予約受付最終日（閉鎖日）」
-- 目的: 閉鎖予定の施設で、固定日（例: 2026-12-28）を最後に予約受付を打ち切る。
--   - closing_date（'YYYY-MM-DD'・この日を含む）までは通常どおり予約可。
--   - 翌日以降はカレンダーに予約枠を出さず、予約作成も弾く。
--   - 予約可能期間（booking_horizon_days・移動窓）とは独立の「固定の上限日」。
--     カレンダーは「6か月先」と「閉鎖日」の早い方で止まる。
-- 後方互換: 既定 NULL（＝閉鎖日なし＝従来どおり）。既存スペースは影響なし。

ALTER TABLE spaces ADD COLUMN closing_date TEXT;
