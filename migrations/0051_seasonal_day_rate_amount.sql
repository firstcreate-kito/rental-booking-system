-- 季節料金ルールに「1日料金（実額・円）」を追加（#18 拡張）。
-- 「☑ この期間は1日料金のみ」と併用し、値がある期間はその実額を1日料金として使う
-- （単価×時間や割増率%の計算を上書き）。NULL＝従来どおり（単価×1日料金時間数＋割増%）。
-- 実額は主に1スペース対象のルールで使う想定（対象スペースの選択は既存機能で制御）。
ALTER TABLE seasonal_pricing ADD COLUMN day_rate_amount INTEGER;
